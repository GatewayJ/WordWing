export function ItemsPage() {
  return (
    <>
      <h2 className="page-title">条目</h2>
      <p className="page-lead">任务列表；逾期使用 error 左边条 3px（DESIGN.md）。</p>
      <div className="card">
        <p>后续：与「定时」关联、行内「提醒」入口跳转 `/todo/schedules`。</p>
      </div>
    </>
  );
}
