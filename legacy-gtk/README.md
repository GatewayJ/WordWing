# Legacy GTK + X11 原型

原仓库根目录的 **Rust + GTK + X11** 实现已迁移到此目录，便于与 **Tauri + React** 主应用并存。

- **包名：** `wordwing-legacy-gtk`
- **运行：** `cd legacy-gtk && cargo run`（需 `DASHSCOPE_API_KEY` 等，见根目录 `README.md` 历史说明）

新功能请在仓库根目录的 Tauri 应用中开发；此目录仅作参考或过渡。
